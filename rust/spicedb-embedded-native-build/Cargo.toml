[package]
name = "spicedb-embedded-native-build"
version = "0.2.4"
edition = "2024"
description = "Shared build script logic for spicedb-embedded platform crates (internal, not published)"
license = "Apache-2.0"
repository = "https://github.com/borkfork/spicedb-embedded"

[lib]
path = "src/lib.rs"
